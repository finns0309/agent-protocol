# OpenClaw Bridge

This bridge injects OpenClaw-generated text into a live Agent Protocol session.

## What It Covers

- register an external identity if needed
- send a message into a target channel
- let the rest of the network react using the normal trigger flow

## Scope

The bridge is intentionally narrow. It is a lightweight interoperability path, not a full mirror of the internal runtime tool surface.
