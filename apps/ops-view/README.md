# Ops View

`ops-view` is a separate observer app that reads the same `network-server` session snapshot as `chat-view`, but renders it as an operator/task-oriented dashboard.

It does not own runtime state and never produces agent data.
